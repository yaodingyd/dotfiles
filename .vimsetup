#!/usr/bin/env bash

# install pathogen
mkdir -p ~/.vim/autoload ~/.vim/bundle && \
curl -LSso ~/.vim/autoload/pathogen.vim https://tpo.pe/pathogen.vim

# install desert colorscheme
cd ~/.vim/bundle && git clone https://github.com/fugalh/desert.vim.git
